server:
  port: 8100

connection:
  default:
    url: localhost:9999
    driver: FlaNium
    isLocal: false

management:
  endpoints:
    web:
      exposure:
        include: "*"
      base-path: "/"